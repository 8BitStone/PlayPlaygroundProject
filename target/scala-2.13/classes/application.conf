# Configuration

play.cache.bindCaches = [] # empty or project caches
# Include the default configurations of the auth library
include "lib.play.base.auth.conf"

play.modules.enabled += "com.informaticon.lib.java.notify.modules.NotifyModule"

# Database configuration
# ~~~~~ 
# You can declare as many datasources as you want.
# By convention, the default datasource is named `default`
#db.default.driver=org.h2.Driver
#db.default.url="jdbc:h2:mem:play"
db {
  default {
    driver = "com.mysql.cj.jdbc.Driver"
    url = "jdbc:mysql://localhost:3306/test?serverTimezone=Europe/Zurich"
    username = "root"
    password = "init_123"
    logSql = false
  }
}

play.i18n.langs = ["de","en"]

play.evolutions.db.default.enabled = false

# Ebean configuration
# ~~~~~
# You can declare as many Ebean servers as you want.
# By convention, the default server is named `default`
ebean.default="models.*"

# Assets configuration
# ~~~~~
"assets.cache./public/stylesheets/bootstrap.min.css"="max-age=3600"

# Number of database connections
# See https://github.com/brettwooldridge/HikariCP/wiki/About-Pool-Sizing
fixedConnectionPool = 9

# Set Hikari to fixed size
play.db {
  prototype {
    hikaricp.minimumIdle = ${fixedConnectionPool}
    hikaricp.maximumPoolSize = ${fixedConnectionPool}
  }
}

# Job queue sized to HikariCP connection pool
database.dispatcher {
  executor = "thread-pool-executor"
  throughput = 1
  thread-pool-executor {
    fixed-pool-size = ${fixedConnectionPool}
  }
}

#notify
notify {
  mail {
    smtp {
        host = "smtp.gmail.com"
        port = 587
        protocol = "tls"
        username = "kevin.riesen@informaticon.com"
        password = "sfsnkvwwealpjpvt"
      }
      from = "ADMIN <noreply@mail.informaticon.com>"
  }
}